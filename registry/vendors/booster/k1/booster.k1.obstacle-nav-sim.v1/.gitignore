
# Webots auto-generated project cache (window state, not source)
simulation/webots/worlds/*.wbproj
simulation/webots/worlds/.*.wbproj

# Bridge runtime state (replay guard database), not source
bridge/*.db
bridge/*.db-wal
bridge/*.db-shm
