[python]
# Point this at the Python environment where `mujoco`, `numpy`, and
# `scipy` are installed (Webots runs controllers with its own default
# python3 otherwise, which usually lacks these packages).
COMMAND = /home/yunaiko/miniconda3/envs/reachy-bounty/bin/python3
